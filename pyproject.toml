[project]
name = "parameter-golf-autoresearch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.6.0",
    "numpy",
    "huggingface_hub",
    "sentencepiece",
    "zstandard",
    "tqdm",
    "safetensors",
]
